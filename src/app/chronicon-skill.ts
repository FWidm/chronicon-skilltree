import {Input} from '@angular/core';

export class ChroniconSkill {
  name: string;
  x: number;
  y: number;
  image: string;
  max_rank: number;
  effect: string;
  skill_requirement: string;
  damage: number;
  family: string;
  min_level: number;
  element: string;
  type: string;
  description: string;
}
